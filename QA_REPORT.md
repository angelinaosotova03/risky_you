# QA_REPORT.md

Самопроверка результатов тестов risky_you по ТЗ «Самопроверка результатов тестов risky_you».
Все 8 игр переведены на чистые функции `compute<Игра>Result()` в `frontend/src/games/*.logic.ts`,
без DOM/сети/`Math.random()` — их можно тестировать напрямую. Автотесты — `vitest`
(`cd frontend && npm test`), 72 теста в 12 файлах, все зелёные на момент сдачи отчёта.

Правило соблюдалось: там, где эталонное значение из ТЗ не совпадало с тем, что даёт
описанный в ТЗ алгоритм, — код не подгонялся молча под ожидание. Разбор — в конце
и в разделе «Вопросы к автору».

## Эталонные случаи (E1–E23)

| № | Игра | Статус | Что было не так | Что исправлено |
|---|---|---|---|---|
| E1 | Шарик | Исправлено | `REWARD=100` вместо «1 монета за качок» из ТЗ — банк был в 100 раз больше ожидаемого | `REWARD=1` в `balloon.logic.ts` |
| E2 | Шарик | Пройдено | — | — |
| E3 | Шарик | Пройдено | Средний банк при стратегии «стоп на 32» = 16 ± 0,2, подтверждено симуляцией | — |
| E4 | Шарик | Пройдено | — | — |
| E5 | Ставка 50/50 | Пройдено | Бисекция уже была верной, λ=2 восстанавливается в 1,95–2,05 | — |
| E6 | Ставка 50/50 | Пройдено | — | — |
| E7 | Ставка 50/50 | Пройдено | — | — |
| E8 | Ставка 50/50 | Пройдено | Флаг «случайные нажатия» на контрольном раунде уже работал | — |
| E9 | Сейчас или потом | Пройдено | — | — |
| E10 | Сейчас или потом | Пройдено | — | — |
| E11 | Сейчас или потом | Расхождение с ТЗ | Эталон «V у 50, k≈0,63» математически недостижим за 5 линейных шагов бисекции в [50,1000] из самого ТЗ | Тест переписан на реально достижимое значение (v≈65, k≈0,479); расхождение — в «Вопросы к автору» |
| E12 | Четыре колоды | Исправлено | Ось «Обучаемость» считала дельту (`learningShift`), а не абсолютную долю из таблицы осей ТЗ — на входе «только колода C» дельта даёт 0%, а не 100% | Добавлена `goodShareRecent` — абсолютная доля выгодных колод в последних 20 картах, она и стала primary-метрикой |
| E13 | Четыре колоды | Исправлено (тем же фиксом) | — | — |
| E14 | Четыре колоды | Пройдено | Сумма блока из 10 карт — ровно ∓250, проверено на массивах `BASE` | — |
| E15 | Доверие | Пройдено | Расчёт уже был верным | — |
| E16 | Доверие | Пройдено | — | — |
| E17 | Ультиматум | Исправлено | MAO считался как `min(принятых)` — для бота «принимает от 3» давал 3, а не 2,5 | MAO = середина между наибольшим отвергнутым и наименьшим принятым (формула из основного ТЗ, была не полностью реализована) |
| E18 | Ультиматум | Расхождение с ТЗ | Эталон MAO=0,5 для бота «принимает всё» не достигается за 3 шага при округлении «половина вверх» — см. «Вопросы к автору» | Формула исправлена (см. E17), тест написан на E19, где то же округление даёт точное совпадение с ТЗ |
| E19 | Ультиматум | Исправлено (тем же фиксом) | MAO у бота «отвергает всё» = 5,5, подтверждено точно | — |
| E20 | Две трети | Построено с нуля | Уровня рассуждения как числа не существовало вовсе, только текстовые бакеты | `computeReasoningLevel`: k = log(guess/50)/log(2/3), 0/1/2 для 50/33/22 (±0,05) |
| E21 | Две трети | Построено с нуля | Метрики S не было — ось «Стратегичность» из таблицы ссылалась на несуществующее поле | `computeS`: S=1 при точном угадывании, S=0,24 при ответе 100 против цели 24 |
| E22 | Ловушки | Исправлено | Поле ответа было `type="number"` — ответы «6 детей» и «шесть» физически невозможно ввести | Поле стало текстовым, добавлена нормализация (`normalizeAnswer`: тримминг, извлечение числа, словарь чисел 0–20) |
| E23 | Ловушки | Пройдено (после фикса E22) | Ответ «10» уже классифицировался как ловушка, а не «прочий» | — |

## Граничные случаи (B1–B13)

| № | Ситуация | Статус | Что было не так | Что исправлено |
|---|---|---|---|---|
| B1 | Шарик: лопнули все 10 | Исправлено | Не обрабатывался отдельно | Среднее по всем раундам + флаг `lowAccuracy` |
| B2 | Шарик: 0 качков везде | Исправлено | Флага не было | Флаг `looksRandom` при накачке 0 во всех раундах |
| B3 | Сейчас/потом, V=1000 | Пройдено | k=0 без деления на 0 — уже было верно | — |
| B4 | Сейчас/потом, V=50 | Исправлено | Годовые показывались как сырое огромное число | Текст «больше 10 000%» при превышении порога |
| B5 | Колоды: закрыл вкладку на 25-й карте | Пройдено | `finish()` не вызывается без всех 40 тяг — результат и не сохраняется структурно | — |
| B6 | Доверие: <50 ответов в базе | Пройдено | Норма из исследований и подпись уже были | — |
| B7 | Ультиматум: принял 2, отверг 4 | Исправлено | Флага противоречия не было вовсе | `looksRandom` при `minAccepted < maxRejected`; MAO=(2+4)/2=3 — тем же фиксом, что и E17 |
| B8 | Две трети: ответ 0 | Построено с нуля | `log(0)` не обрабатывался (метрики не существовало) | Явная ветка `guess<=0 → REASONING_CAP=4`, без вызова `Math.log(0)` |
| B9 | Две трети: ввод вне 0–100 | Пройдено структурно | — | Поле — слайдер с `min/max`, недопустимый ввод физически невозможен; кнопка «Готово»/подсказка из ТЗ относятся к текстовому полю, которого здесь нет |
| B10 | Ловушки: пустой ответ по таймеру | Исправлено | Таймер был декоративным, по истечении ничего не происходило | По истечении — автосабмит с категорией «прочий», игра продолжается |
| B11 | Все игры: повторное прохождение | Пройдено | Усреднение и сплошная точка уже работали (`accuracy.ts`) | Добавлен тест `accuracy.test.ts` на этот механизм |
| B12 | Все игры: двойной клик | Исправлено (1 из 8) | В «Четырёх колодах» не было вообще никакой защиты от повторного клика — 7 остальных игр уже гасили повтор через `locked` | Добавлен короткий (250мс) дебаунс на клик по карте |
| B13 | Все игры: перезагрузка страницы | Пройдено | Результаты в `localStorage`, читаются заново при каждой загрузке | — |

## Симуляция ботами (по 1000 сессий на игру)

| Игра | Статус | Комментарий |
|---|---|---|
| Шарик | Пройдено | min(цель=1) < random < max(цель=55) по средней накачке; <50мс/сессию |
| Ставка 50/50 | Пройдено | принимает всё < random < отвергает всё по λ; параметрический бот (λ=2) восстанавливает параметр |
| Сейчас или потом | Пройдено | всегда «потом» < random < всегда «сейчас» по k |
| Четыре колоды | Пройдено | только A (0%) < random < только C (100%); обучающийся бот — доля в последнем блоке из 10 карт выше, чем в первом |
| Доверие | Пройдено | отправляет 0 < random < отправляет всё; параметрический бот (5 из 10) даёт ровно 50% |
| Ультиматум | Пройдено | принимает всё < отвергает всё по MAO; параметрический бот (порог 3) даёт MAO=2,5, как в E17 |
| Две трети | Пройдено | далёкий ответ < random < точный ответ по S |
| Ловушки | Пройдено | всегда ловушка (0%) < random < всегда верно (100%); параметрический бот (P=0,5) восстанавливает ≈50% на 3000 попытках |

Во всех восьми — без NaN/Infinity, все сессии укладываются в 50мс.

## Направления осей, процентили, радар

| Проверка | Статус | Что было не так | Что исправлено |
|---|---|---|---|
| Направление оси — Рискованность (Шарик) | Пройдено | — | — |
| Направление оси — Избегание потерь (Ставка) | Пройдено | — | — |
| Направление оси — Терпеливость (Сейчас/потом) | Пройдено | `invert:true` уже стоял верно | — |
| Направление оси — Обучаемость (Колоды) | Исправлено | Метрика была дельтой, не долей (см. E12) | `traits.ts`: `range:[0,100]`, без инверсии, метрика `goodShareRecent` |
| Направление оси — Доверчивость (Доверие) | Пройдено | — | — |
| Направление оси — Справедливость (Ультиматум) | Пройдено | — | — |
| Направление оси — Стратегичность (Две трети) | Исправлено | Оси ссылалась на метрику `distance`/`guess` с `invert:true` — работало, но не соответствовало метрике S из таблицы ТЗ | `traits.ts`: метрика `s`, `range:[0,1]`, без инверсии; спецкейс в `radar-data.ts` для «Двух третей» убран — S считается всегда (см. E21) |
| Направление оси — Вдумчивость (Ловушки) | Пройдено | — | — |
| Процентиль 0–100, средний ранг у равных значений | Исправлено | `backend/app/routers/norms.py`: percentile = 100×below/total — у игроков с одинаковым результатом систематически недооценивался (считались «хуже» равных себе) | percentile = 100×(below + equal/2)/total |
| Подпись «предварительно» до 100 прохождений | Пройдено | `settings.min_norm_sample=100` совпадает с порогом из ТЗ; в интерфейсе — полая точка + подпись «черновая прикидка» (та же суть, другая формулировка) | — |
| Радиус точки = процентиль/100 | Пройдено | `polarPoint` в `profile.ts` — уже точно по формуле | — |
| Пунктир «среднее» на 50-м процентиле | Пройдено | `AVERAGE_LEVEL=50` — константа по определению процентиля | — |
| Непройденная ось — сера, без точки; полигон от 3 игр | Пройдено | — | — |
| Процентиль в тексте результата = точка на радаре | Исправлено | Экран результата (`pages/test.ts`) считал процентиль по последней попытке, радар (`radar-data.ts`) — по среднему по всем попыткам. При повторном прохождении числа расходились | `pages/test.ts` теперь использует то же усреднение (`averageMetric`), что и радар |

## Тексты результатов

| Проверка | Статус | Что было не так | Что исправлено |
|---|---|---|---|
| Нет незаполненных `{…}`/`undefined`/`null`/`NaN` | Пройдено (после фиксов выше) | Опциональные поля (`beauty.ts`: `average`, `target`, `guess2`) уже были защищены проверками — перепроверено | — |
| Склонение 1/2–4/5–20, включая 11–14 и 21/22/25 | Исправлено (2 игры) | «Ультиматум»: «X монет» всегда в родительном падеже; «Ловушки»: «X раз» без варианта «раза» | `pluralLabel`/`plural` из `plural.ts`; добавлен `plural.test.ts` с явными кейсами на 11–14 и 21/22/25 |
| Округление: λ до 1 знака, % до целых, деньги до 10 ₽, тысячи через пробел | Исправлено (2 места) | «Ловушки»: `reflectionScore` был с 1 знаком после запятой (33,3%); «Сейчас или потом»: годовая ставка — с 1 знаком; «Ставка 50/50» и «Сейчас или потом» — денежные суммы в результатах не округлялись до 10 ₽ | Проценты — `Math.round`; денежные суммы в `summarize()` — через `roundTo(n, 10)` |
| Смысл фразы совпадает с числом («доверие окупалось», «выдержал 15 секунд») | Исправлено (1 игра) | «Доверие»: «окупалось» сравнивало возврат с утроенной суммой, а не с отправленной, как требует ТЗ | Добавлена метрика `avgSent`, сравнение `avgReturned > avgSent` |
| Особые фразы срабатывают точно в своих случаях (ответ 0 в «Двух третях», «простил машину» в «Ультиматуме») | Исправлено (1) / Пройдено (1) | «Две трети»: фраза про глубину рассуждения пропадала, когда на сервере уже было реальное среднее (показывались только «Средний ответ»/«Цель»); «Ультиматум»: «простил машину» уже срабатывало верно, но флаг `looksRandom` (B7) не был подключён к тексту вовсе | «Две трети»: фраза о глубине показывается всегда; «Ультиматум»: добавлено сообщение про противоречивые ответы |
| Тексты не выходят за 360px | Пройдено (по코드у) | Проверено по CSS: у `.result-compare`/`.result-details` нет `white-space:nowrap`/фиксированной ширины — длинный текст оборачивается. Визуально в браузере на 360px не прогонялось | — |
| Карточка для шеринга = экран результата | Пройдено | Обе фразы — одна и та же переменная `sharePhrase`; радар в обоих местах строится через общий `loadAxes` | — |

## Вопросы к автору

1. **E11 («Сейчас или потом»)** — эталон «бот всегда выбирает „сейчас“ → V у 50, k≈0,63» математически недостижим за 5 линейных шагов бисекции в диапазоне [50,1000], как буквально описано в основном ТЗ: при таком боте бисекция сходится к V=65 (k≈0,479), а не к 50. Либо увеличить число шагов, либо расширить нижнюю границу диапазона, либо посчитать эталон как «около 0,48», а не «около 0,63». Тест написан на фактически достижимое значение, эталон не менялся молча.

2. **E18 («Ультиматум»)** — эталон «бот принимает всё → MAO=0,5» не достигается за 3 шага при округлении «половина вверх» (`Math.round`, как в исходной реализации бисекции 1–5 из ТЗ): реально получается MAO=1. Интересно, что для симметричного случая (E19, «отвергает всё») та же схема с тем же округлением даёт ровно эталонные 5,5 — асимметрия возникает из-за направления округления при приближении к нижней границе диапазона. Формула MAO (середина между отвергнутым и принятым) исправлена и проверена на E17/E19/B7, где она точна; для E18 остаётся либо округление «половина вниз» для этой ветки (но тогда «сломается» E19), либо признать 0,5 недостижимым при целочисленной бисекции 1–5 за 3 шага и поправить эталон на 1.

3. **Репозиторий не является git-репозиторием** — в ТЗ сказано «каждое исправление — отдельный коммит с номером проверки в сообщении», это физически невозможно без `git init`. Список из 25 исправлений (эталонные случаи, тексты, направления осей) зафиксирован построчно в этом отчёте — по ним можно восстановить историю коммитов, если репозиторий инициализируют.

4. **Проценты «предварительно» до 100 прохождений** — в ТЗ сказано «подпись «предварительно»», в интерфейсе сейчас другая формулировка («черновая прикидка, мало данных» + полая точка на радаре). Смысл тот же, слово другое — не стал переименовывать текст без явного запроса, раз он уже согласован с остальным UI игры.

5. **B9 («Две трети», ввод вне 0–100)** — в ТЗ описано поведение текстового поля («кнопка неактивна, подсказка под полем»), а в игре используется слайдер с `min=0 max=100`, у которого недопустимый ввод физически недостижим. Это тот же случай, что и B5 у «Четырёх колод» — правильное поведение уже гарантировано механикой интерфейса, а не отдельной проверкой значения.

## Итог

- Проверок из ТЗ: **65** (23 эталонных + 13 граничных + 8 симуляций ботами + 8 направлений осей + 6 процентиль/радар + 7 текстов).
- Исправлено: **25** (список — построчно в таблицах выше).
- Прогонов потребовалось: **~9** — по одному на каждую из 8 игр (эталонные случаи + логика) плюс один сквозной прогон на симуляцию ботами, направления осей и тексты результатов вместе.
- Открытых вопросов к автору: **5**, из них 2 — фактические расхождения между эталонными числами ТЗ и его же собственным описанием алгоритма (E11, E18), не исправленные молча.
